//look at API documentation to find out how to write this part of the code

function recipeDOMObject(recipeJSON, user) {
  const card = document.createElement('div');
  card.className = 'recipe card';


  return card;
}